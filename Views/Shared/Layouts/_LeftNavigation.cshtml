@model IPageViewModel<SitePageData>

@{ Layout = "~/Views/Shared/Layouts/_Root.cshtml"; }

<div class="row">
    <div class="col-md-4">
        <div class="row">
            <div class="col-md-4">
                @{Html.RenderPartial("SubNavigation", Model);}
                @RenderSection("RelatedContent", false)
            </div>
        </div>
    </div>

    <div class="col-md-8">
        @RenderBody()
    </div>
</div>
